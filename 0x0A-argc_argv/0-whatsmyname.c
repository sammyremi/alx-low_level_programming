#include <stdio.h>
#include <stdlib.h>
/**
 * main - name of my function
 * @argc: count
 * @argv: vector
 * Return: return 0 if success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
