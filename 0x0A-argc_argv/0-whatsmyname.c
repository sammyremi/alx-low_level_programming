#include"main.h"
#include <stdio.h>
/**
 * main - name of my function
 * @argc: count
 * @argv: vector
 * Return: return 0 if success
 */
int main(int argc, char *argv[])
{
	if (argc < 2)
	{
		printf("%s\n", argv[0]);
	}

	return (0);
}
