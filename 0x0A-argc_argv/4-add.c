#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * main - add positive numbers
 * @argc: count
 * @argv: vector
 * Return: return 0 if success
 */
int main(int argc, char *argv[])
{
	int i;
	int sum = 0;

	if (argc == 1)
	{
		printf("0\n");

		return (0);
	}
	else
	{
		for (i = 1; i < argc; i++)
	}
	
	return (0);
}
