#include "main.h"

/**
 * print_numbers - Entry point
 *
 * return:number to be checked
 *
 * Return: Always 0 (Success)
 */
void print_numbers(void)
{
	for (int c = '0'; c <= '9'; c++)
	{
		_putchar(c);
	}
	_putchar('\n');
}
